[phases.setup]
nixPkgs = ["python313", "gcc", "swig", "cmake", "pkg-config"]
aptPkgs = ["build-essential", "libopenblas-dev", "liblapack-dev"]

[phases.install]
cmds = [
    "pip install --upgrade pip setuptools wheel",
    "pip install -r requirements.txt"
]

[start]
cmd = "python searchbot_ver0.5.0.1.py"
